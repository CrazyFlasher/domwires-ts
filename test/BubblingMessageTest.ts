import {Suite} from "mocha";
import {expect} from "chai";

describe('BubblingMessageTest', function (this: Suite)
{
    it('testBubblingFromBottomToTop', () =>
    {
        expect(true).true;
    });
});